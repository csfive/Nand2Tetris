/**
 * Exclusive-or gate:
 * if ((a and Not(b)) or (Not(a) and b)) out = 1, else out = 0
 */
CHIP Xor {
    IN a, b;
    OUT out;

    PARTS:
        Nand(a=a, b=b, out=w);
        Nand(a=a, b=w, out=wa);
        Nand(a=b, b=w, out=wb);
        Nand(a=wa, b=wb, out=out);
}
